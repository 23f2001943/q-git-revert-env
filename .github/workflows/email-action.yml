name: Email Check Action

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: 23f2001943@ds.study.iitm.ac.in
        run: echo "Hello, world!"
